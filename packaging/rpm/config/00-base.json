{
  "$schema": "https://opencode.ai/config.json",
  "model": "ollama/MODEL_ID",
  "provider": {
    "ollama": {
      "name": "Ollama (LAN)",
      "options": { "baseURL": "http://OLLAMA_HOST:OLLAMA_PORT/v1", "apiKey": "ollama" },
      "models": { "MODEL_ID": { "limit": { "context": 65536, "output": 8192 } } }
    }
  }
}
